<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.i7.eatit.domain.user.dao.UserMapper">

    <resultMap id="userInfoMap" type="com.i7.eatit.domain.user.dto.UserInfoDTO">
        <id property="member_id" column="MEMBER_ID"/>
        <result property="email" column="EMAIL"/>
        <result property="password" column="PASSWORD"/>
        <result property="name" column="NAME"/>
        <result property="nickname" column="NICKNAME"/>
        <result property="loginMethod" column="LOGIN_METHOD"/>
        <result property="phoneNumber" column="PHONE_NUMBER"/>
        <result property="createdDate" column="CREATED_DATE"/>
        <result property="withdrawalStatus" column="WITHDRAWAL_STATUS"/>
        <result property="reportedCount" column="REPORTED_COUNT"/>
    </resultMap>

    <select id="findByUsername" resultMap="userInfoMap" parameterType="string">
        SELECT * FROM TBL_MEMBER WHERE EMAIL = #{username}
    </select>

    <select id="findUsersByGroupId" resultMap="userInfoMap" parameterType="long">
        SELECT u.*
        FROM TBL_MEMBER u
        JOIN TBL_GROUP_MEMBER gm ON u.MEMBER_ID = gm.USER_ID
        WHERE gm.GROUP_ID = #{groupId}
    </select>

</mapper>
